---
layout: mypost
title: 字符串函数
categories: [MySQL, 单行函数]
author: runtao.zhong
date: 2026-08-06
---

1.ascll()返回第一个字母ascll值
2.char_length() 中字算一个字符
   length()中字算三个字符(utf-8中)
3.concat()连接
  concat_ws(分隔符,a,b,...)
4.insert(字符串,第几个,长度,插入内容)插入
5.replace(字符串,替换部分,替换内容)替换
6.upper()  ucase()大写
7.lower() lcase()小写
8.left(字符串,取的数量)
9.right(字符串,取的数量)
10.lpad(字符串,数量,补的内容)右对齐
11.rpad(字符串,数量,补的内容)左对齐
12.trim()去除首尾侧空格
ltrim()去除左侧空格
rtrim()去除右侧空格
trim("o"form "hello")去除所谓o
13.repeat(内容,次数)
14.space(n)返回n个空格
15.strcmp(a,b)返回1,a大;0一样大;-1,b大
16.substr(字符串,a,b)返回第二个开始的b个数
17.locate(内容,字符创)返回位置对应的数字
18.elt(数,str1,str2,...)返回数字对应的str
19.flelt(s,str1,str2,...)返回字符串的所在位置
20.nullf(a,b)ab相等返回null,否则返回a